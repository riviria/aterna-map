import { largos } from "./largos/location";
import { largosDetails } from "./largos/details";

export const midLocations = [largos];

export const midLocationDetails = {
  [largos.id]: largosDetails,
};
